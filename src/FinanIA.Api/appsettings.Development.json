{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
  // To configure the Gemini API key for local development, run:
  // dotnet user-secrets set "Gemini:ApiKey" "<your-api-key>"
  // Never commit the real API key to source control.
}